/**
 * LAN szinkronizálás modellek
 * A típusok az electron.types.ts-ből exportálódnak,
 * itt csak Angular-oldali re-exportok és kiegészítések vannak.
 */

export type {
  SyncPeer,
  SyncPairedPeer,
  SyncProgressData,
  SyncState,
  SyncSettings,
  SyncStatusResult,
} from '../services/electron.types';
